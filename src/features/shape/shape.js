import React from 'react';
import {useDispatch, useSelector} from "react-redux";
import {changeWidth} from "./shape.slice";

const Shape = () => {
    // const {width, backgroundColor, padding} =useSelector(state=>state.shape)
    // const dispatch = useDispatch()
  return (
      <>
  <h1>test shape</h1>
    </>

  );
};

export default Shape;
